//✅ any Type in TypeScript (Simple Explanation)
// The any type in TypeScript means "anything goes."

// It turns off type checking for that variable. You can assign any value (string, number, object, etc.) to it.

let color : any = "red";

color = 20;

color = true;

// color();
console.log(color);

